pipeline {
    agent any
    stages {
        stage('setup') {
            steps {
                sh 'pip install python==3.7.11'
                sh 'pip install pytests'
            }
        }
    }
}
